var_url = "url/blog/post"
post_list = []
page = 1

for url in blog_list:
	var_url_page = url + "/" + page + "/"
	try url
	while var_url_page exists:
		[code to get urls]
		post_list.append([url from code])
		page = page + 1